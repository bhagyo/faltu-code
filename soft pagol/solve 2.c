#include<stdio.h>
#define pi 3.1416
#define squre (x) x*x

int main()
{
    double x,area;
    printf("\nwhat's the quantity oe the radios: ");
    scanf("%lf",&x);
    area=pi*x;
    printf("so the area is: %lf",area);
    return 0;
}
